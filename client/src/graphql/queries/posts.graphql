query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMorePosts
    posts {
      id
      title
      textSnippet
      points
      createdAt
      updatedAt
      creator {
        id
        username
      }
    }
  }
}